#include<stdio.h>
#include<math.h>

int main()

{
    int i=2,j,a,b,c,d,m,z,k;
    d=b*i;
    m=d-4*a*c;
    printf("enter the no a:");
    scanf("%d",&a);
    printf("enter the no b:");
    scanf("%d",&b);
    printf("enter the no c:");
    scanf("%d",&c);
    if (d>0){
        printf("real roots\n");
        { z=-b+(m)/2*a;
          k=-b-sqrt(m)/2*a;
          printf("first root %d\n",z);
          printf("second root %d",k);

        }}
    else if
    else
        {printf("not real roots");}
    return 0;
}