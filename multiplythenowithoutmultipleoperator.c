//programe to show the no is multiplied by the no 
//without usinhg the multiple operator
#include<stdio.h>
int main()
{
    int i,z,m;
    printf("enter the no:");
    scanf("%d",&i);
    z=(i<<2)+(i);
    printf("%d",z);
}